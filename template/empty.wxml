<template name="empty">
  <view class="empty-container">
    <image lazy-load class="empty-img" mode="aspectFit" src="/static/empty.png"></image>
    <view class="empty-text">{{ text || '暂无内容' }}</view>
  </view>
</template>